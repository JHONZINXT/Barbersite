# Copie este arquivo para .env.local (que já é ignorado pelo git) e preencha
# com os valores reais. Nunca cometa .env.local no repositório.

# Painel do Supabase → Project Settings → API
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Senha simples pra acessar /agenda (invente uma qualquer)
AGENDA_ACCESS_CODE=
